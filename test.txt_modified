int sum = 0;
int i;
for(i = 1; i < 10; i += 2)
{
     sum += i;
     sum += i - 1;
}

int j;
for(j = 8; j > 0; j -= 2)
{
     sum -= j;
     sum -= j + 1;
}
/* Remainding Iterations */
for(j = 1; j > 0; j -= 1)
{
     sum -= j;
}
